version: "3.9"

services:
  nginx:
    image: nginx:stable-alpine
    ports:
      - "5173:80"
    networks:
      - midnodejs
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    restart: unless-stopped

  mysqldb:
    image: mysql:8.0
    container_name: mysqldb
    environment:
      MYSQL_ROOT_PASSWORD: 123456
    ports:
      - "3306:3306"
    volumes: 
      - mysql_data:/var/lib/mysql
      - ./scripts/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - midnodejs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-p123456"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    command: >
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci

  rabbitmq:
    image: rabbitmq:4-management
    container_name: rabbitmq
    networks:
      - midnodejs
    environment:
      RABBITMQ_DEFAULT_USER: admin
      RABBITMQ_DEFAULT_PASS: admin
    ports:
      - "5672:5672"
      - "15672:15672"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "rabbitmqctl", "status"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  users:
    image: duyphan020/midnodejs-users:v1
    build: ./services/user
    networks:
      - midnodejs
    ports:
      - "3001:3001"
    depends_on:
      mysqldb:
        condition: service_healthy
    environment:
      PORT: ${USERS_PORT}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      SERVICE_TOKEN: ${SERVICE_TOKEN}
      MYSQL_DB_HOST: mysqldb
      MYSQL_DB_USER: ${MYSQL_DB_USER}
      MYSQL_DB_PASS: ${MYSQL_DB_PASS}
      MYSQL_DB_NAME: ${MYSQL_USERDB_NAME}
      MYSQL_DB_PORT: ${MYSQL_DB_PORT}

  tuitions:
    image: duyphan020/midnodejs-tuitions:v1
    build: ./services/tuition
    networks:
      - midnodejs
    ports:
      - "3002:3002"
    depends_on:
      mysqldb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    environment:
      PORT: ${TUITIONS_PORT}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      SERVICE_TOKEN: ${SERVICE_TOKEN}
      AMQP_URL_DOCKER: amqp://admin:admin@rabbitmq:5672
      MYSQL_DB_HOST: mysqldb
      MYSQL_DB_USER: ${MYSQL_DB_USER}
      MYSQL_DB_PASS: ${MYSQL_DB_PASS}
      MYSQL_DB_NAME: ${MYSQL_TUITIONDB_NAME}
      MYSQL_DB_PORT: ${MYSQL_DB_PORT}
    restart: on-failure

  payments:
    image: duyphan020/midnodejs-payments:v1
    build: ./services/payment
    networks:
      - midnodejs
    ports:
      - "3003:3003"
    depends_on:
      mysqldb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      users:
        condition: service_started
      tuitions:
        condition: service_started
    environment:
      PORT: ${PAYMENTS_PORT}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      SERVICE_TOKEN: ${SERVICE_TOKEN}
      AMQP_URL_DOCKER: amqp://admin:admin@rabbitmq:5672
      MYSQL_DB_HOST: mysqldb
      MYSQL_DB_USER: ${MYSQL_DB_USER}
      MYSQL_DB_PASS: ${MYSQL_DB_PASS}
      MYSQL_DB_NAME: ${MYSQL_PAYMENTDB_NAME}
      MYSQL_DB_PORT: ${MYSQL_DB_PORT}
      EMAIL_USERNAME: ${EMAIL_USERNAME}
      EMAIL_APP_PASSWORD: ${EMAIL_APP_PASSWORD}
      USER_SERVICE_HOST: http://users:${USERS_PORT}
      ARCJET_ENV: ${ARCJET_ENV}
      ARCJET_KEY: ${ARCJET_KEY}
    restart: on-failure

  gateway:
    image: duyphan020/midnodejs-gateway:v1
    build: ./gateway
    networks:
      - midnodejs
    ports:
      - "3000:3000"
    depends_on:
      users:
        condition: service_started
      tuitions:
        condition: service_started
      payments:
        condition: service_started
    environment:
      GATEWAY_PORT: ${GATEWAY_PORT}
      USERS_URL: http://users:${USERS_PORT}
      TUITIONS_URL: http://tuitions:${TUITIONS_PORT}
      PAYMENTS_URL: http://payments:${PAYMENTS_PORT}

  client:
    image: duyphan020/midnodejs-client:v1
    build: ../client
    networks:
      - midnodejs
    depends_on:
      gateway:
        condition: service_started

networks:
  midnodejs:
    driver: bridge
  
volumes:
  mysql_data:
